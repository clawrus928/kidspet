# 一般情況不需要任何環境變數:
# 後端伺服器會同時派發前端與 /api,前端預設「同源」呼叫 API。
#
# 只有當前端與後端 API 分開部署在不同網址時,才需要設定下面這個:
# VITE_API_BASE=http://你的伺服器位址:3000
#
# 後端可調整的環境變數(啟動 node server/index.mjs 時):
#   PORT=3000                  伺服器埠號
#   DATA_FILE=./data/families.json   資料檔位置
